# Tube MPC Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add LTV Tube MPC wrapping existing Torque MPC with ancillary LQR and tightened constraints for robustness to prediction uncertainty and tracking error.

**Architecture:** Nominal Torque MPC (tightened bounds) → CasADi autodiff linearization along nominal → backward Riccati for K_k → ancillary correction τ = τ_nom + K(x - x_nom).

**Tech Stack:** CasADi (autodiff), SciPy (DARE), existing RNEA symbolics from mpc_dynamics.py.

**dynamics_dict structure (verified):** exposes `mass_matrix_fn(q)→M`, `coriolis_fn(q,qd)→C*qd`, `gravity_fn(q)→g`, `rnea_fn(q,qd,qdd)→τ`.

---

## Tasks 1-3 (parallel), 4-6 (sequential)

See conversation for full task details. Key dependency: Tasks 1,2,3 → Task 4 → Task 5 → Task 6.
